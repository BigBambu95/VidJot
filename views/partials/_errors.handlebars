{{#if error}}
    <div class="alert alert-danger col-md-6 mb-2 offset-md-3">{{error}}</div>
{{/if}}

{{#each errors}}
  <div class="alert alert-danger col-md-6 mb-2 offset-md-3">{{text}}</div>
{{else}}

{{/each}}